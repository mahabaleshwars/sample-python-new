name: Test Python 3.13.8t on Windows ARM64

on:
  workflow_dispatch:

jobs:
  test-314-then-314t:
    name: Setup 3.13.8 then 3.13.8t (reproduces issue)
    runs-on: windows-11-arm
    steps:
      - name: Setup Python 3.13.8
        uses: actions/setup-python@v5
        with:
          python-version: '3.13.8'
          architecture: arm64
          allow-prereleases: true
      
      - name: Verify 3.13.8 installation
        shell: bash
        run: |
          echo "=== After installing 3.13.8 ==="
          python --version
          which python
      
      - name: Setup Python 3.13.8t (this should fail)
        uses: actions/setup-python@v5
        with:
          python-version: '3.13.8t'
          architecture: arm64
          allow-prereleases: true
      
      - name: Check what happened
        shell: bash
        run: |
          echo "=== After attempting to install 3.14t ==="
          python --version
          which python

  test-314t-alone:
    name: Setup 3.13.8t alone (should work)
    runs-on: windows-11-arm
    steps:
      - name: Setup Python 3.13.8t
        uses: actions/setup-python@v5
        with:
          python-version: '3.13.8t'
          architecture: arm64
          allow-prereleases: true
      
      - name: Verify installation
        shell: bash
        run: |
          python --version
          python -c "import sys; print(f'GIL enabled: {sys._is_gil_enabled() if hasattr(sys, \"_is_gil_enabled\") else \"Unknown\"}')"
